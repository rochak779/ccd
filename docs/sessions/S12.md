# S12 session record

Status: Pass (`session-12-pass`)

## Scope

- Implementation: S12 — request extraction and reply matching
- Product: PRD §§17.3, 20, 32; T1–T2 and U2–U3

## Outcome

Validated request extraction creates one C-14 parent with four stable item IDs and preserves source/confidence. Existing imported C-14 is linked instead of duplicated. Replies match deterministically by `In-Reply-To`, provider thread and `References`; explicit IDs and ambiguous relationships remain suggestions requiring confirmation.

## Verification

| Gate | Command or evidence | Result |
|---|---|---|
| Domain tests | `node --experimental-strip-types --test tests/domain/s11-s12-ingestion.test.ts` | Pass — 4/4 |
| Lint | `npm run lint` | Pass |
| Type check | `npm run typecheck` | Blocked by concurrent S13 Buffer typing errors; S11/S12 produced no diagnostics |
| Negative / recovery | Invalid extraction IDs and ambiguous matching boundaries | Pass |

## Known limitations

- Confirmation is exposed as a domain control; UI presentation belongs to the inbox/request-detail surface.

## Handoff

S13 can consume accepted attachment sources and S15 can evaluate only confirmed/deterministic request relationships.
