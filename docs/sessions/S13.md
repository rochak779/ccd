# Session 13 — Workbook parsing and addressable evidence

Status: `session-13-pass`

- Added byte-level XLSX parsing behind a reusable parsed-source contract.
- Preserved sheet/table structure, headers, raw and displayed values, inferred period/unit, and formula/cached-result provenance.
- Added strict cell/range validation plus source-hash and locator-content checks before cached extraction can be used.
- Corrupt workbooks and formulas without cached results emit no evidence value.

Verification: `npm run test:domain -- --test-name-pattern workbook` and `npm run typecheck`.
